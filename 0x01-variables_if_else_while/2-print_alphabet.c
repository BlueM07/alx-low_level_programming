#include <stdio.h>
/**
* main - gives alphabet
* a is alphabet
*
* main function produces the whole alphabet in lowercase
* description header
* the return value a is the alphabet
*/
int main(void)
{
char a = 'a';
putchar(a);
return(a);
}
