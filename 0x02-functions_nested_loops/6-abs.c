#include "main.h"
/**
 *_abs - print _alphabet
 * @n: parameter
(*
* Description: prints a specific string
(* section header)
* Return: the demanded string
*/
int _abs(int n)
{
	int r = 0;

	if (n < 0)
	{
		n = n * -1;
	}
	r = n;
return (r);
}
