#include "main.h"
/**
 * jack_bauer - print _alphabet
(*
* Description: prints a specific string
(* section header)
* Return: the demanded string
*/
void jack_bauer(void)
{
	int a, b, c ,d;


	if (n < 0)
	{
		n = n * -1;
	}
	r = n % 10;
	_putchar(r + 48);
	return (r);
}
