#include "main.h"
#include <stdio.h>

/**
 * main - function
 * @argc: counter
 * @argv: vector
 * Return: prog name
 */
int main(int argc __attribute__((unused)), char *argv[])
{
	printf("prog name is: %s/n", argv[0]);
	return (0);
}
