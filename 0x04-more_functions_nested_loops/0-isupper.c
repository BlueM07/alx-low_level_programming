#include "main.h"
#include <ctype.h>
/**
 * main - checks for uppercase character
 * parameter
(*
* Description: prints a specific string
(* section header)
* Return: the demanded string
*/
int main(void)
{
char ch;
if (isupper(ch))
{_putchar('1');
}
else 
{
	_putchar ('0');
}
}
