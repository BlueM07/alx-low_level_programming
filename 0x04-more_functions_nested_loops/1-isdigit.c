#include "main.h"

/**
 * _isdigit - decription of the function
 * @c: parameter
 * Return: function result
 */
int _isdigit(int c)
{
	if (c >= '0' && c <= '9')
	{
		return (1);
	}
	else
	{
		return (0);
	}
}

