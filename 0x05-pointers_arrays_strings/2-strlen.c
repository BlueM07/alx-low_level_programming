#include "main.h"

/**
 * _strlen - decription of the function
 * @s: parameter
 * Return: function result
 */
int _strlen(char *s)
{
	int len;
	int i;

	for (i = 0; s[i] != '\0'; i++)
	{
		len = i + 1;
	}
	return (len);
}
