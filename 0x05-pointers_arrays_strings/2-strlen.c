#include "main.h"

/**
 * _strlen - decription of the function
 * @s: parameter
 * Return: function result
 */
int _strlen(char *s)
{
	int len;
	int i;

	for (i = 0; i <= s[i]; i++)
	{
		len = i;
	}
	return (len);
}
