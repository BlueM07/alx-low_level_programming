#include "main.h"

/**
 * _strlen - decription of the function
 * @s: parameter
 * Return: function result
 */
int _strlen(char *s)
{
	int len;
	int i;
	char s[n];

	for (i = 0; i < n; i++)
	{
		len = i;
	}
}
